export const TAB_BAR_HEIGHT = 64;

/** Stoopy size inside the bottom Explore button (fully contained). */
export const STOOPY_SIZE = 52;

/** Visible bar height excluding safe-area inset. */
export const STOOPY_BAR_HEIGHT = 92;
